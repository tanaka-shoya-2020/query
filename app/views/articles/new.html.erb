<div class="container-fluid">
  <h2 class="text-center">新規投稿</h2>
  <%= form_with model: @article, url: articles_path, local: true do |f| %>
  <div class="col-xs-10 col-xs-offset-1 col-md-offset-2 col-md-8 col-md-offest-2">
    <div class="form-group">
      <%= f.label :タイトル %>
      <%= f.text_area :title, placeholder: "タイトルを入力してください(最大50文字)",class: "form-control",autofocus: true %>
    </div>

    <div class="form-group">
      <%= f.label :テキスト %><br />
      <% text = "### 投稿したい記事
以下に投稿したい内容を記入してください

(例)rubyにおけるform_withについて

#### 使用した言語(ruby ,html ,css など)

```ruby
ここにコードを記入
```

#### 自分が新しく学んだものは何であるのか

#### どの部分を理解するのに苦労したか"%>
      <%= f.text_area :text, value: "#{text}", placeholder: "投稿したい内容を30字から10000字で記入してください。

記入はマークアップ言語を用いて書くと綺麗に書くことができます。", rows: "20",class: "form-control",autocomplete: true, id: "editor" %>
    </div>
  </div>

  <script>
    var simplemde = new SimpleMDE({ 
      element: document.getElementById("editor"),
      tabSize: 4});
  </script>
  <script>
      marked.setOptions({
            sanitize: true,
            sanitizer: escape,
            breaks : true
        });
  </script>

  <div class="actions">
    <%= f.submit "投稿する",class: "btn btn-primary btn-lg btn-block" %>
  </div>
<% end%>
</div>